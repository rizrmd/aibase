layout {
    pane split_direction="vertical" {
        pane {
            command "bash"
            args "-c" "cd backend && bun --hot src/orpc/server.ts"
        }
        pane {
            command "bash"
            args "-c" "cd frontend && bun run dev"
        }
    }
}
